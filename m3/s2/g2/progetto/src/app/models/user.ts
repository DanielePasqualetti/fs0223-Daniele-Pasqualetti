export class User {
  nome: string;
  cognome: string;
  pizza: string;

  constructor(nome: string, cognome: string, pizza: string) {
    this.nome = nome;
    this.cognome = cognome;
    this.pizza = pizza;
  }
}
